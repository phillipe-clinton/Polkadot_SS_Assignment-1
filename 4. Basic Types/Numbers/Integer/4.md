🌟🌟

```rust

question->
/* Fill the blanks to make it work
fn main() {
    assert_eq!(i8::MAX, __); 
    assert_eq!(u8::MAX, __); 

    println!("Success!");
} */

solution code->
fn main() {
    assert_eq!(i8::MAX, 127); 
    assert_eq!(u8::MAX, 255); 
    
    println!("Success!");
}

output-> Success!

changes-> filled in 127 and 255 as the max value of i8 and u8 respectively.

```
