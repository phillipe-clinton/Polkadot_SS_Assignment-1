5. 🌟 Indexing starts at 0

```rust
question->
/* fn main() {
    let arr = ['a', 'b', 'c'];
    
    let ele = arr[1]; // Only modify this line to make the code work!

    assert!(ele == 'a');

    println!("Success!");
} */

solution code->
fn main() {
    let arr = ['a', 'b', 'c'];

    let ele = arr[0];

    assert!(ele == 'a');
    println!("Success!");
}

output-> Success!

changes-> let ele = arr[0] because index starts at 0

```
